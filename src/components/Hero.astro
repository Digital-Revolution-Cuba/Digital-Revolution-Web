---
import Accordeon from '../components/AccordeonSlider.astro';
import '../styles/glass-card.css';
---

<section
  class="hero-section bg-brand-backgroundGlobal text-text-primary relative flex min-h-screen w-full items-center justify-center overflow-hidden"
>
  <div
    class="hero-container z-10 flex w-full max-w-7xl flex-col items-center gap-8 px-6 py-16 lg:flex-row lg:gap-12 lg:px-12 lg:py-0"
  >
    <!-- Left content -->
    <div class="hero-content flex w-full flex-col gap-6 justify-center lg:w-1/2">
      <h2 class="hero-title">
        Descubre <br /> Talentos
      </h2>
      <p class="hero-description">
        Encuentra y conecta con personas creativas para intercambiar ideas y
        colaborar en proyectos innovadores. Explora eventos locales, talleres y
        conferencias donde puedas conocer a individuos con intereses similares
        de diversas disciplinas.
      </p>

      <div class="flex justify-start">
        <button class="btn-primary"> Explora Talentos </button>
      </div>
    </div>

    <!-- Right content - Accordeon -->
    <div class="hero-accordeon flex w-full justify-center md:w-1/2">
      <Accordeon />
    </div>
  </div>
</section>

<style>
  .hero-section {
    padding: clamp(2rem, 5vw, 4rem) clamp(1rem, 3vw, 2rem);
  }

  .hero-container {
    align-items: center;
  }

  .hero-title {
    font-family: 'Impact', 'Arial Black', sans-serif;
    line-height: 0.95;
    letter-spacing: 0.02em;
    color: var(--color-text-primary, #ffffff);
    /* Fluid scale: controlled range 48px - 80px */
    font-size: clamp(3rem, 2rem + 4vw, 5rem);
    text-shadow: 0px 4px 4px #00000040;
    display: inline-block;
    transform: scaleX(1.15);
    transform-origin: left;
  }

  .hero-description {
    color: var(--color-text-secondary, #a0a0a0);
    line-height: 1.5;
    /* Fluid text size */
    font-size: clamp(1rem, 0.6rem + 0.5vw, 1.375rem);
    max-width: 500px;
  }

  /* Desktop adjustments */
  @media (min-width: 1024px) {
    .hero-section {
      min-height: 100vh;
      padding: 0 2rem;
    }

    .hero-container {
      height: 100vh;
    }

    .hero-title {
      font-size: clamp(3.5rem, 3rem + 2vw, 5rem);
    }
  }

  /* Tablet adjustments */
  @media (min-width: 768px) and (max-width: 1023px) {
    .hero-container {
      flex-direction: column;
      text-align: center;
    }

    .hero-content {
      align-items: center;
    }

    .hero-description {
      text-align: center;
    }

    .hero-cta {
      margin: 0 auto;
    }
  }

  /* Mobile adjustments */
  @media (max-width: 767px) {
    .hero-section {
      min-height: auto;
      padding: 4rem 1rem;
    }

    .hero-container {
      gap: 3rem;
    }

    .hero-title {
      font-size: clamp(2.5rem, 8vw, 3.5rem);
    }

    .hero-description {
      font-size: clamp(0.9rem, 3.5vw, 1.125rem);
    }
  }
</style>
